# Catalog

Machine-readable contract for TutorOS. Regenerated by `python scripts/build_catalog.py` from `tutor-platform-demo.html` `S` plus planned APIs in that script.

- `screens.json` — 47 screens (closed set)
- `modules.json` — A1–G5
- `flows.json` — template tours t1–t6
- `apis.json` — planned `/api/v1` paths
- `entities.json` — spine vs later
- `ports.json` — vendor adapters
- `env.json` — names mirrored in `.env.example`
- `embed.js` — loaded by `tutor-platform-architecture.html`

Do not invent screen ids here that are missing from the demo.
