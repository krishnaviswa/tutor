# Models

SQLAlchemy 2 for PostgreSQL. Local app uses Compose Postgres (README §1). pytest in-memory SQLite only. `workspace_id` on business tables. `topics` / `taxonomies` — never syllabus. README §5. Mirror of `.cursor/rules/database.mdc`.
