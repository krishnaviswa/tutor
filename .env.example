# Names only. All providers default to mock in CI. Never commit real secrets.

DATABASE_URL=postgresql+asyncpg://tutoros:tutoros@localhost:5432/tutoros
SMS_PROVIDER=mock
EMAIL_PROVIDER=mock
VIDEO_PROVIDER=mock
STORAGE_PROVIDER=local
PAYMENTS_STUDENT_PROVIDER=mock
PAYMENTS_PLATFORM_PROVIDER=mock
PUSH_PROVIDER=mock
WHATSAPP_PROVIDER=mock
SPECIFY_FEATURE_DIRECTORY=specs/001-platform-architecture
