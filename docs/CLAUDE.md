# Docs

README is product SoT. `docs/examples/` is not requirements. Mirror of `.cursor/rules/docs-and-api.mdc`.

Hub: `product-viewer.html`. Work log: `work-log.html`. Same-change: catalog + architecture + README + hub status + work-log line when screens/APIs/tables/ports change.
