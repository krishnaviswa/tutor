# Slice / spec template (TutorOS)

Prefer Spec Kit: `specs/<nnn-name>/spec.md` + `plan.md`. This file is the PM/Architect checklist if you are not using Specify CLI.

Copy to `specs/` or fill via `/speckit.specify`.

Status: Draft → Specified → In Progress → Testing → Accepted

Do not invent screen ids. Do not treat Biology as the product.
