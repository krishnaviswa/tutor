---
description: FastAPI conventions
globs: backend/**/*.py
alwaysApply: false
---

# FastAPI (002-sim-spine)

Thin routers, logic in `services/`, REST under `/api/v1`, `require_roles()`, QuotaGuard after authz (paid channels). Mock ports default.

See README §3 and `backend/CLAUDE.md`.
