---
description: Implement tasks.md — blocked until Specified + human OK
---

Refuse if checklists have unchecked items or status is not Specified. Refuse for `001-platform-architecture` until the user accepts the architecture HTML. Then implement tasks in order on a feature branch, never main.
